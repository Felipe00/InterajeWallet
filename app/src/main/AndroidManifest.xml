<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.interaje.mypersonalwallet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_star"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MyBalance">

        </activity>
        <activity android:name=".activities.SaqueActivity"></activity>
        <activity android:name=".activities.DepositoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>